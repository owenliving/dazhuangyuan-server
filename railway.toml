# Railway 部署配置 - 大状元后端服务
# 此文件放在 dazhuangyuan-server/ 目录下

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/dazhuangyuan.jar"
healthcheckPath = "/api/ad/publicConfig"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
